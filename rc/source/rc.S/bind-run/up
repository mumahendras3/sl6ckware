###############################
# Bind mount /run to /var/run #
###############################

if -tn { mountpoint -q /var/run }
foreground { mount -o bind /run /var/run }
importas -u ? ?
ifelse { [ $? != 0 ] } { exit $? }
echo "bind-run: /run bound on /var/run"
