###################################################################
# Start HCI devices listed in /etc/default/bluetooth at boot time #
###################################################################

foreground { echo "Starting oneshot service:  hciconfig" }
pipeline { grep ACTIVE_HCI_DEVICES_ON_BOOT= /etc/default/bluetooth }
pipeline { cut -f2 -d\" }
forstdin -pnd" \n" DEV
importas -u dev DEV
hciconfig $dev up
