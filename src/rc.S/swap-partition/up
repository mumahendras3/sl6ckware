################################################################
# Enable all swap partitions that are configured in /etc/fstab #
################################################################

foreground { echo "Starting oneshot service:  swap-partition" }
pipeline { findmnt --fstab -o SOURCE -t swap -rne }
forstdin -pd"\n" SOURCE
importas -u source SOURCE
if { [ -b $source ] }
swapon $source
