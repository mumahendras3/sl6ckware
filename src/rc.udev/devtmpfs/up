##########################
# Mount devtmpfs on /dev #
##########################

if -tn { mountpoint -q /dev }
ifelse { grep -wq devtmpfs /proc/filesystems }
{ mount -nt devtmpfs -o size=8M devtmpfs /dev }
mount -no mode=0755,size=8M -t tmpfs tmpfs /dev
