############################
# Mount devpts on /dev/pts #
############################

foreground { echo "Starting oneshot service:  devpts" }
if { mkdir -p /dev/pts }
if -tn { mountpoint -q /dev/pts }
mount -n /dev/pts
