#######################################################
# Remount the root filesystem with read-write enabled #
#######################################################

foreground { echo "remount-root: Remounting root device with read-write enabled..." }
mount -vnwo remount /
