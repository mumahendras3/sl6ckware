#####################################
# Load user-selected console keymap #
#####################################

# Prepend every output with the service's name
pipeline -w { sed -u "s/^/keymap: /" }
fdmove -c 2 1
loadkeys "<insert_keymap_name_here>"
