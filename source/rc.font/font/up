###################################
# Load user-selected console font #
###################################

# Prepend every output with the service's name
pipeline -w { sed -u "s/^/font: /" }
fdmove -c 2 1
setfont -v "<insert_font_name_here>"
