############################################################################
# Load the mixer settings and OSS compatibility (if enabled) for ALSA (the #
# Advanced Linux Sound Architecture)                                       #
############################################################################

# Load the ALSA mixer settings
alsa-mixer

# Load the ALSA OSS kernel modules to support OSS applications (better leave
# this service disabled and use the aoss wrapper script instead - see
# rc.alsa-oss/alsa-oss for details)
#alsa-oss
