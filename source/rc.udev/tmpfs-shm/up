##########################################
# Mount the tmpfs filesystem on /dev/shm #
##########################################

# Prepend every output with the service's name
pipeline -w { sed -u "s/^/tmpfs-shm: /" }
fdmove -c 2 1
if { mkdir /dev/shm }
mount -vn /dev/shm
