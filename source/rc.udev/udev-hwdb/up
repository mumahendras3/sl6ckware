###########################################################
# Update the hardware database index (/etc/udev/hwdb.bin) #
###########################################################

if { echo udev-hwdb: Updating hardware database index: udevadm hwdb --update }
udevadm hwdb --update
