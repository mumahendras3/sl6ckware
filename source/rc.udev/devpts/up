###########################################
# Mount the devpts filesystem on /dev/pts #
###########################################

# Prepend every output with the service's name
pipeline -w { sed -u "s/^/devpts: /" }
fdmove -c 2 1
if { mkdir /dev/pts }
mount -vn /dev/pts
